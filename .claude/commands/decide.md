---
description: Decision brief — options, tradeoffs, a recommendation. No action (Layer 2, metered)
allowed-tools: Bash, Read, Grep, Glob
argument-hint: "<question>"
---

METERED command. Answer `$ARGUMENTS` as a DECISION, not an action.

1. Gather the relevant code, data, and prior ledger context.
2. Return: the question restated, 2-4 real options, each option's tradeoffs, and one recommendation with its strongest counter-argument (lead with the objection).
3. Mark each claim validated / inferred / speculative.

Record it in the ledger under pending_decisions with awaiting_operator=true. Change no code, take no outward action.
